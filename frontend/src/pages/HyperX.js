import React from 'react';
import Typography from '@mui/material/Typography';

function HyperX() {
  return (
    <div style={{ textAlign: 'center', marginTop: '100px' }}>
      <Typography variant="h2">HyperX</Typography>
    </div>
  );
}

export default HyperX;
